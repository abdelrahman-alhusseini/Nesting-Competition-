MICHAEL & SON NESTING CHAMPIONS - WORKING CHECKPOINT

Included in this checkpoint:
- Latest responsive light login screen
- Latest square login hero artwork
- Supabase project connection in lib/main.dart
- Fixed app_controller.dart null-safety issues
- Existing agent/admin pages preserved
- The unapproved new Admin Dashboard redesign is NOT included

On the new laptop:
1. Install Flutter and Chrome if needed.
2. Open THIS folder in VS Code (the folder containing pubspec.yaml).
3. Open a terminal here.
4. Run:

   flutter clean
   flutter pub get
   flutter analyze
   flutter run -d chrome

The Supabase publishable client key is already configured in lib/main.dart.
Never put a Supabase secret/service-role key in the Flutter app.
