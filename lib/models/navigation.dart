enum AgentPage {
  dashboard,
  bookings,
  drawCards,
  cardReveal,
  myCards,
  useSpecialCard,
  leaderboard,
  activityFeed,
  howToPlay,
}

enum AdminPage {
  dashboard,
  approvals,
  users,
  testDraw,
  cardReveal,
  adjustPoints,
  auditHistory,
  settings,
}
